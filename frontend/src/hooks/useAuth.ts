import { useAuth as useAuthContext } from "../contexts/AuthContext";

// Export the useAuth hook from context
export const useAuth = useAuthContext;

// Additional auth-related hooks can be added here
export default useAuth;
